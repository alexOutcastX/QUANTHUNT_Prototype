# Build output
build/
dist/
*.spec.bak

# Python
__pycache__/
*.pyc
venv/
.venv/

# Node
node_modules/

# Local/env
.env
*.log

# runtime caches
fund_cache.json
fund_cache.json.tmp
scan_cache.json
scan_cache.json.tmp
index_cache.json
graph_cache.json
graph_cache.json.tmp
broker_token.json

# runtime database
quanthunt.db
quanthunt.db-wal
quanthunt.db-shm
# The generated session-signing key. Committing it would publish the key that
# every member cookie is signed with.
quanthunt.db.session-key

# Local throwaway static server used only for headless UI verification
# fake_server.py is the committed E2E fixture (see e2e/smoke.js) — no longer ignored
mb_screen.json
pattern_screen.json
mom_screen.json
backtests.json
backtests.json.tmp
sectors_cache.json
sectors_cache.json.tmp
mobile/dist-precompressed/
bse_codes.json
ipo_cache.json

# Prebuilt screener payloads (snapshot.py). Machine-local, rebuilt twice a day,
# and a megabyte of market data that would be stale the moment it was committed.
screener_snapshots.json
screener_snapshots.json.tmp
